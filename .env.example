# API
PORT=4000
WEB_ORIGIN=http://localhost:5173

# Supabase public values are safe for the browser when RLS is enabled.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_API_URL=http://localhost:4000

# Server-side Supabase values. Never expose these with VITE_.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# OpenAI will be added in a later phase. Never expose this in the browser.
OPENAI_API_KEY=
